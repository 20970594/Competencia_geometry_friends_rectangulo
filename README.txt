El trabajo esta siendo realizado en Visual Studio y la instalacion ya esta hecha, por lo
que al abrir el archivo GeometryFriendsAgents.sln solo debe presionar iniciar 
en la parte superior para correrlo

La razon por la que el algoritmo utilizado es A* es porque ese el algoritmo que utilizaba
la competencia que usamos como base y como aun no tenemos el algoritmo Dijkstra estamos
usando ese por el momento